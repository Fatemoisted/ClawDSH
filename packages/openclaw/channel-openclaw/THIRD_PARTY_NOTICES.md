# Third-party notices

This package interoperates with and supervises an unmodified OpenClaw release artifact. OpenClaw is distributed under the MIT License; its license text is included as `LICENSE.openclaw`.

The ClawDSH Provider implementation is distributed under the package's `LICENSE` file. `LICENSE.openclaw` applies only to the separately acquired OpenClaw artifact.

The OpenClaw host and optional channel extensions remain separately installed artifacts. Their exact versions, integrity values, source ownership, and support status are recorded by `tools/openclaw-channel-host/`. Loading an external extension requires explicit deployment allowlisting and does not transfer its license or platform obligations to ClawDSH.
