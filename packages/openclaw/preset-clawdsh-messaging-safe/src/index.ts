/**
 * Static messaging-safe Agent preset package.
 *
 * The published runtime module intentionally exports no behavior; preset
 * discovery consumes the package-owned YAML and prompt resources.
 * @module @clawdsh/dsh-preset-messaging-safe
 */

import type {} from '@clawdsh/dsh-soul'

export {}
