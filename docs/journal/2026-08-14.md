# 开发日志 2026-08-14

## 今日完成

- **项目立项**：确定用 DeepSeek Harness (dsh) 的 Cordis 底盘重建 OpenClaw 个人助手功能集，项目定名 **ClawDSH**。
- **仓库初始化**（非 Fork，按发起人要求）：clone `deepseek-ai/deepseek-harness` 到 `/Users/mac/ClawDSH`；`origin` 改名为 `upstream`；基线 `47f943859b`（v0.1.0-rc.5）。
- **品牌层改造**：根 `package.json` name → `clawdsh`；README / AGENTS.md（CLAUDE.md 为其符号链接）置顶 ClawDSH 项目规范段，上游内容原样保留。
- **插件骨架**：`packages/openclaw/`（preset-openclaw、channel-core、channel-telegram、soul、memory、skills-hub、automation + `_template/`）。骨架不含 `package.json`（防 tsdown 通配误伤，接入时再启用——见该目录 README）。
- **文档体系**：`docs/specs/roadmap.md`（目的+实施方案）、`docs/adr/0001`（基座形态）、`docs/adr/0002`（渠道 seam）、`docs/matrix/parity.md`（对齐矩阵 v1）、`docs/standards/`（upstream-sync / plugin-contract / pr-policy / naming）。
- **环境验证**：Node v22.22.2、git 2.50.1；corepack 不可用，`npm i -g pnpm@11.7.0` 后 `pnpm install` 成功（20.7s，lefthook hooks 已装；两个 examples 的 bin 警告因 lib 未构建，正常）。

## 排查记录：首次 typecheck 失败与修复

- **现象**：`pnpm run typecheck` 在 tsdown host 阶段报 `[clawdsh] Cannot find entry: lib/types/{index,invariant,startup}.js`。
- **根因**：tsdown 的 workspace 扫描以**目录**为粒度（`onlyDirectories: true`），`packages/openclaw/*` 骨架目录被扫中；目录内无 package.json → 向上寻根到根包（name=`clawdsh`）→ 用根配置解析入口失败。此前"骨架不放 package.json"策略只防住了 pnpm，未防住 tsdown。
- **修复**：根 `tsdown.config.ts` 的 workspace 改为对象形式并显式 `exclude: ['packages/openclaw/**']`（exclude 会整体替换 tsdown 内置默认值，四条默认模式已原样保留）。该改动已写入 ADR-0001 决策 4 的"构建编排最小豁免"。

## 环境事实（供后续会话）

- 项目根：`/Users/mac/ClawDSH`；上游远程：`upstream`；开发分支：`clawdsh`（**尚未创建**）。
- pnpm 全局 bin 不在非交互 shell PATH：用 `$(npm config get prefix)/bin/pnpm` 或 `export PATH="$(npm config get prefix)/bin:$PATH"`。

## 私有远程与推送（当日完成）

- 创建 `clawdsh` 分支，首个 commit `d9e691f38e`（框架初始化，29 文件）；
- 添加 origin = `https://github.com/Fatemoisted/ClawDSH.git`，推送 `master`（上游镜像）与 `clawdsh` 两分支，均成功；
- 排查记录：① pre-push 钩子找不到 pnpm → 推送前需 `export PATH="$(npm config get prefix)/bin:$PATH"`；② GitHub token 缺 `workflow` scope 被拒 → 用户补权限后重推成功；
- GitHub 凭据已存入 macOS 钥匙串，后续 `git push` 免 token；移除方法见 `docs/standards/upstream-sync.md`。

## 阶段 0 Spike（当日完成 ✅）

- **调研结论**：soul 挂 `ctx.systemPrompt` 接缝（`section({name,order,text,complete?})` 有序段装配；order 0=部署人格、100-199=工具指引；`complete` 段=唯一提示），作用域用 `dsh-scope` 的 `createScope`/`scopeOf`；与上游 `dsh-persona` 同构（scope-only 行）。
- **实现**：`packages/openclaw/soul`（`@clawdsh/dsh-soul`，replace/append 双模式 + 灵魂文件加载 + `src/invariant.ts` 配套）；注册进 tsconfig.base paths、tsconfig.host.json references、tsdown 排除名单移出。
- **验证**：契约测试 10/10（替换/叠加/回卷/双作用域/文件加载/三类 fail-loud/上下文抑制）；全量 `pnpm typecheck` 绿；`--profile openclaw --dump-config` 冒烟通过（dsh-base+headless 层叠 + 我们的 persona 覆盖生效）。
- **踩坑记录**：① vitest 强制每个测试包要有 `src/invariant.ts` 配套（test-invariants 检查）；② schemastery 枚举需 `z.union([z.const(...)])` 才能匹配接口联合类型；③ cordis.patch.yml 必须是顶层数组的 patch entries。
- **阶段 0 结论**：接缝假设成立——OpenClaw 能力可以零上游改动地以插件形态落在 dsh 上。**项目继续。**

## 阶段 1 基线选型（当日完成 ✅）

- **方法**：partial clone（blob:none）到 `/tmp/openclaw-ref`；按窗口枚举 first-parent commit 量与 tag；对候选逐一点数 `ls-tree -r` 文件数/体积，并展开顶层与 `src/` 结构做功能集合边界检查。
- **数据**：first-parent 月 commit 量 2025-11: 288 → 2025-12: 2,072 → 2026-01: 5,097 → 2026-02: 7,560（爆炸起点在 2 月）。
- **候选对比**：2025-12-31（1197 文件）→ **v2026.1.5（1537 文件，1.6MB）** → v2026.1.15（3367，翻倍，+whatsapp/memory/channels 抽象/extensions）→ v2026.1.20（4041）→ v2026.1.30（4543，+fly/render 部署矩阵）。
- **定稿：`v2026.1.5`（`197b8f7c3b`）**。理由：首个发布 tag（1.5-1/2/3 小版本只修 bug）；网关+5 渠道（discord/telegram/imessage/signal/slack）+cron+sessions+tui/wizard 核心完整；最瘦；无 bloat。功能补全参考 v2026.1.15（`9c4c9c5edd`），gateway 雏形参考 2025-12-31（`f03605d8ae`）。
- 矩阵 v2 定稿（docs/matrix/parity.md），含每个功能域的基线出处路径。
- 备注：OpenClaw 的 `src` 是普通 tree（子模块只有 Peekaboo）；`git ls-tree <ref> <dir>` 不带 `-r` 只显示条目本身，需 `-r` 或子树 hash 展开。

## 国内平台渠道决策（当日，发起人确立）

- **原则**：只实现 OpenClaw 上游有出处的功能（摸着石头过河）；不自行发明上游没有的功能域。
- **调研结论**（OpenClaw 最新 main）：官方有 `extensions/feishu`（2026-02-03 引入，`2483f26c23`→`0223416c61`，v2026.2.12 起发布）；**无任何微信系渠道**（`extensions/tencent` 是腾讯云 LLM provider）。
- **决策**：① 飞书 = 发起人第一优先，**提前到阶段 2** 与 Telegram 并行做 `ctx.channels` seam 验证（ADR-0002 备选渠道定稿为飞书）；② 微信系（企业微信/公众号/个人微信）与钉钉/QQ 一律不做核心包——原则性排除，上游将来新增 wecom 时再跟进（决策记录在 packages/openclaw/channel-wechat/README.md）。

## 阶段 2 渠道垂直切片（当日完成 ✅）

- **channel-core**（`@clawdsh/dsh-channel-core`，commit `3d7db1da33`）：新增 `ctx.channels` seam——adapter 注册表（id 唯一、注销回卷）+ `channel/inbound` 入站路由 + per-thread agent turn 驱动（`agents.create`→`followup`→`whenIdle`→`sessions.flush`→扫 `assistant/message` 取回复）+ `channel/outbound` 回投；per-thread tail-chain 串行化。
- **channel-telegram**（`@clawdsh/dsh-channel-telegram`，commit `1ff441855e`）：getUpdates 长轮询入站（offset 推进、at-least-once 幂等）+ sendMessage 出站。
- **channel-feishu**（`@clawdsh/dsh-channel-feishu`，commit `84c8fc352c`，发起人第一优先）：`node:http` webhook 明文入站（ACK-first、challenge 回显、v1/v2 双格式解析、uuid/event_id 幂等去重）+ tenant_access_token 缓存出站（im/v1/messages）。`encryptKey` 配置存在但 fail-loud（加密未实现）。
- **preset 接线**（commit `947943475b`）：profile patch `insert` 三条渠道行（默认 `disabled: true`，凭证后置）；`--dump-config` 冒烟解析出三行。
- **验证**：三包契约测试全绿（channel-core 4、telegram 4、feishu 7）+ 全量 `pnpm typecheck` 绿 + `--dump-config` 冒烟通过。
- **踩坑记录**：① feishu 测试 `noUnusedParameters` 需 mock 未用参数加下划线前缀（`_init`）；② Service 包用 `this.ctx.logger`（非 `this.logger`）；③ `ctx.effect` 回调同步执行、返回 disposer；④ `exactOptionalPropertyTypes` 下可选字段条件展开。
- **文档**：ADR-0002 定稿为 Accepted + 结论「双渠道验证通过」；`feature-channel-core.md` 五段式规格；矩阵三行翻 implemented；上游提案草稿 `docs/upstream-proposal/ctx-channels.md`（新目录，已入品牌段 allowed 列表）。
- **doc-sync 收口**：四个 openclaw 包 README 补 `## Model Experience` + `## Known Limitations and Deferred Work`；feishu `createWebhookState` 补 `@returns`；`config-catalog.md` 再生成（纳入 feishu/telegram/soul 配置）；ADR-0002 与上游提案的 `ts` 代码块补 `Context` import。剩余失败非阶段 2 可解：① `cordis-catalog`/`doc-graphs`——`channel/inbound`/`channel/outbound` 引用未分类类型 `ChannelMessage`，分类表在只读的 `scripts/gen-cordis-catalog.ts`，须待上游 `ctx.channels` PR 合入后在 `linkedTypePages` 登记；② translation pairing（全仓双语）、markdown wrap（naming/upstream-sync/feature-memory）、doc budgets（AGENTS.md 超 1900 词）均为预存在项。

## 待办（下一会话起点）

1. 真实 e2e（需凭证）：飞书 app_id/app_secret、Telegram botToken、`DEEPSEEK_API_KEY`——解开对应渠道行 + 填 Config，验证「消息进 → 人格化 agent → 回复出」真实闭环；
2. ~~向上游提 `ctx.channels` 提案 PR~~ 已撤销（发起人 2026-08-14 决定跳过上游 PR、快速推进）；`ctx.channels` 作为 ClawDSH 自有 seam 长期保留，设计记录见 docs/upstream-proposal/ctx-channels.md；
3. 阶段 2 深读：OpenClaw v2026.1.5 的 `src/gateway/` 与 `src/agents/` identity 机制（对照 matrix 基线出处列）；
4. GitHub 仓库设置：把默认分支设为 `clawdsh`（仓库默认是 `main`）。

## 会话记忆

- AI 会话记忆指针：`~/.claude/projects/-Users-mac/memory/openclaw-dsh-rebuild.md`（只存指针与共识，内容以本仓库文档为准）。
